/**
 * Internal service boundary. User programs are delegated through {@code SandboxRuntime} to an
 * isolated container and are never invoked directly in this JVM.
 */
package com.tutorplatform.worker;
